# DeepPresenter

### Installation
```bash
playwright install
# might missing dependencies here, don't forget to use conda/apt/brew to install them
cd docker; docker build -t desktop-commander-deeppresenter .
```


